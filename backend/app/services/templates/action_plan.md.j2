# SEO Action Plan

**Site:** {{ site_url }}  
**Date:** {{ generated_at }}  
**Current Score:** {{ score }}/100  
**Target Score:** 85+/100

---

## Plan Overview

| Metric | Value |
|--------|-------|
| Duration | {{ plan_summary.plan_duration_weeks|default(12) }} weeks |
| Total Tasks | {{ plan_summary.total_action_items|default(0) }} |
| Technical Tasks | {{ plan_summary.technical_tasks|default(0) }} |
| Content Tasks | {{ plan_summary.content_tasks|default(0) }} |

{% if plan_summary.expected_outcomes %}
### Expected Outcomes
{% for outcome in plan_summary.expected_outcomes %}
- {{ outcome }}
{% endfor %}
{% endif %}

---

## Quick Wins (Immediate Impact)

These can be implemented quickly for immediate SEO improvement:

{% if quick_wins %}
{% for win in quick_wins %}
- [ ] **{{ win.title }}** [{{ win.severity|capitalize }}]
  {% if win.suggested_fix %}- {{ win.suggested_fix }}{% endif %}

{% endfor %}
{% else %}
No quick wins identified - all issues require standard effort.
{% endif %}

---

## Priority Issues

{% for issue in prioritized_issues[:20] %}
### {{ loop.index }}. {{ issue.title }}

| Attribute | Value |
|-----------|-------|
| Severity | {{ issue.severity|severity_emoji }} {{ issue.severity|capitalize }} |
| Category | {{ issue.category }} |
| Pages Affected | {{ issue.affected_count }} |

{% if issue.suggested_fix %}
**Action Required:**
> {{ issue.suggested_fix }}
{% endif %}

{% if issue.affected_urls %}
**Sample URLs:**
{% for url in issue.affected_urls[:3] %}
- `{{ url|truncate_url(60) }}`
{% endfor %}
{% endif %}

---

{% endfor %}

## Implementation Timeline

{% for phase_name, actions in actions_by_phase.items() %}
### {{ phase_name }}

{% for action in actions %}
#### [ ] {{ action.task }}

- **Timeline:** Week {{ action.week_start|default('?') }} - {{ action.week_end|default('?') }}
- **Effort:** {{ action.effort|default('medium')|capitalize }}
- **Impact:** {{ action.expected_impact|default('medium')|capitalize }}
{% if action.description %}
- **Details:** {{ action.description }}
{% endif %}
{% if action.target_keywords %}
- **Keywords:** {{ action.target_keywords|join(', ') }}
{% endif %}

{% endfor %}
{% endfor %}

---

{% if content_calendar %}
## Content Calendar

| Week | Title | Type | Keywords | Status |
|------|-------|------|----------|--------|
{% for item in content_calendar %}
| {{ item.week }} | {{ item.title|truncate(30) }} | {{ item.content_type }} | {{ item.target_keywords[:2]|join(', ') }} | {{ item.status }} |
{% endfor %}

{% endif %}

---

## Implementation Checklist

### Week 1-2: Foundation
- [ ] Fix all critical issues
- [ ] Address high-priority technical problems
- [ ] Set up tracking and monitoring

### Week 3-4: Technical Optimization
- [ ] Implement performance improvements
- [ ] Fix URL structure issues
- [ ] Resolve internal linking problems

### Week 5-8: Content Improvement
- [ ] Update existing content based on audit findings
- [ ] Create new content per calendar
- [ ] Optimize on-page elements

### Week 9-12: Refinement
- [ ] Address remaining medium/low issues
- [ ] Monitor and adjust based on results
- [ ] Plan next audit cycle

---

## Success Metrics

Track these metrics to measure progress:

| Metric | Baseline | Target | Timeline |
|--------|----------|--------|----------|
| SEO Score | {{ score }} | 85+ | 12 weeks |
| Critical Issues | {{ prioritized_issues|selectattr('severity', 'equalto', 'critical')|list|length }} | 0 | 2 weeks |
| High Priority Issues | {{ prioritized_issues|selectattr('severity', 'equalto', 'high')|list|length }} | 0 | 4 weeks |
| Organic Traffic | Baseline | +20% | 12 weeks |

---

*Plan generated by SEOman v2.0*
